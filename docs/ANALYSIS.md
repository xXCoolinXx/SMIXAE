# SMIXAE Analysis

## Analysis Workflow

```
1. Generate probing datasets     smixae generate-probing-data generate
2. Train SMIXAE                  smixae train ...
3. Probe experts                 smixae probe single / all-datasets
4. Interpret 3-D scatters        Open results/*/probe/top_experts.html in a browser
5. Steer                         smixae steer main (see docs/STEERING.md)
```

The probe step is the core of the analysis. It runs the LLM + SMIXAE on a labeled dataset, scores every active expert, and writes ranked HTML visualizations.

---

## Probing Datasets

All datasets live in `datasets/probing/` as CSVs with `Sentence` and `Label` columns. Generated by `smixae generate-probing-data generate`.

| Dataset | Concept | Labels | Type |
|---------|---------|--------|------|
| `weekdays.csv` | Day of week | Mon–Sun (7 classes) | Discrete cyclic |
| `hours.csv` | Hour of day | 0–23 (24 classes) | Discrete cyclic |
| `temperatures.csv` | Temperature category | cold / cool / warm / hot | Ordinal |
| `time_units.csv` | Time granularity | second / minute / hour / day / month / year | Ordinal |
| `body_parts.csv` | Body region | head / torso / arm / leg / foot | Categorical |
| `living_things.csv` | Biological category | plant / animal / fungus / bacterium | Categorical |
| `colors.csv` | Basic colors | red / orange / yellow / green / blue / purple / black / white | Categorical |
| `months.csv` | Month of year | Jan–Dec (12 classes) | Discrete cyclic |
| (newline) | Chars since last newline | integer count | Continuous |

Template-based generation: each sentence embeds a concept value (e.g. `"It is Monday."`) using a random template and a random name from a pool of 30 names. The label is the concept value. De-duplication ensures no two sentences are identical.

---

## Scoring Metrics

### Fisher Discriminant Ratio

**What it measures**: Class separability — how well the expert's 3-D bottleneck activations cluster by label.

**Formula** (for a 3-D bottleneck):
```
F = tr(S_B) / tr(S_W)
```
where `S_B` is the between-class scatter matrix and `S_W` is the within-class scatter matrix of the bottleneck activations.

**When to use**: Any dataset with discrete class labels. A high Fisher score means the expert has learned a geometry that separates the classes cleanly — the bottleneck positions for class A are far from class B in 3-D space.

**Adjusted Fisher score**: `F * (n_seen_classes / n_total_classes)` — penalises experts that are only active for a subset of classes in the dataset (e.g., an expert that only fires on Monday sentences but not other weekdays should score lower even if its Monday cluster is tight).

### KNN Continuity

`Expert.evaluate_manifold()` finds k-nearest neighbors of each token in **bottleneck space** (Euclidean distance), then computes the average cosine similarity between those neighbors' **LLM residual stream activations**. It is a bridging metric: bottleneck proximity → LLM-space coherence. It does not measure continuity within the bottleneck itself.

Used for unsupervised expert discovery. In practice it tends to surface linear directions rather than genuinely nonlinear manifold structure.

`sort_key("continuity")` ranks by this score.

### Expert Regression Probing

`Expert.evaluate_regression()` runs supervised regression or classification on the 3-D bottleneck activations using hypotheses defined in `DatasetConfig.regression_hypotheses`. Supported modes:

| Mode | Task | Score |
|------|------|-------|
| `linear` | Linear regression | R² |
| `ridge` | Ridge regression | R² |
| `logistic` | Binary classification | Balanced accuracy |
| `multinomial` | Multi-class classification | F1-macro |

Classification modes use `StratifiedKFold` cross-validation. After evaluation, `Expert.best_regression_name` and `Expert.best_regression_score` hold the top-scoring hypothesis.

`sort_key("regression")` ranks by `best_regression_score`. All four sort modes: `"fisher"`, `"adjusted_fisher"`, `"continuity"`, `"regression"`.

### Newline Metrics

See the table in [CLAUDE.md — Analysis Metrics](../CLAUDE.md) for definitions of `decode_r2`, `encode_linear_r2`, `encode_periodic_r2`, and `periodic_gain`.

**Interpretation guide**:
- `encode_periodic_r2 > 0.5` and `periodic_gain > 0.1`: strong ring or spiral structure for newline distance.
- `decode_r2 > encode_linear_r2`: the decoder is amplifying the linear signal; the expert is contributing to the residual in a linearly decodable way.
- `periodic_gain < 0`: the expert encodes newline distance linearly, not periodically.

---

## HTML Output Guide

### `top_experts.html`

A ranked table of the top-N experts scored by Fisher or continuity. Each row has:
- Expert ID
- Score value
- Thumbnail of the 3-D scatter (hover for label)
- Links to the full per-expert page

Open in a browser. The Plotly figures are interactive — rotate, zoom, and hover over points to see label and sentence.

### `dim_analysis_expert{id}.html`

Full analysis page for a single expert. Contains:
- Full 3-D scatter of all active samples, colored by label
- Mean scatter (one sphere per class mean)
- Label annotations positioned around the class means
- Score summary at the top

The 3-D scatter is the raw bottleneck output — coordinates are exactly the 3 bottleneck dimensions, no PCA or reduction.

---

## Extending Analysis

### Adding a New Probing Dataset

1. Add a generator function to `src/analysis/generate_probing_data.py`:
   ```python
   def generate_my_concept(n_samples: int = 5000) -> pd.DataFrame:
       """..."""
       templates = ["...", "..."]
       labels = ["class_a", "class_b", ...]
       return _enumerate_unique(templates, labels, ("concept",), n_samples)
   ```
2. Call it inside `generate()` and save the output:
   ```python
   df.to_csv(OUTPUT_DIR / "my_concept.csv", index=False)
   ```
3. Add an entry to `datasets/probing/dataset_config.json` with the appropriate colorscale and config fields (see CLAUDE.md — Dataset Config Schema).

### Expert Filtering

`ExpertFilterConfig` (in `src/analysis/utils.py`) controls which experts are included in analysis:

| Field | Default | Meaning |
|-------|---------|---------|
| `active_threshold` | `1e-5` | Minimum L2 norm for an expert activation to count as active |
| `min_active_fraction` | `0.10` | Drop experts active on fewer than this fraction of tokens |
| `max_points` | `1000` | Randomly downsample experts exceeding this point count (0 = no cap) |

Pass a `ExpertFilterConfig` instance to `get_sae_activations()` to override defaults.

### Adding a New Scoring Metric

Add a method to the `Expert` class in `src/analysis/utils.py`:

```python
def evaluate_my_metric(self) -> None:
    """Compute and store my new metric on self.activations."""
    # self.activations: (n_active_samples, d_bottleneck)
    # self.labels: (n_active_samples,) int tensor or None
    self.my_metric_score = ...
```

Then add `"my_metric"` to the `sort_key()` method and include it in the HTML template in `build_dataset_html()`.
